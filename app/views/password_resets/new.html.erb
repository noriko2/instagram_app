<% provide(:title, "Forget password") %>

<div class="row">
  <div class="col-md-6 col-md-offset-3 column-space" style="background:white; border:1px solid #dae5f0;">
    <h1>パスワードの再設定</h1>
    <p>ご登録のメールアドレスをご入力ください。<br>
       パスワード再設定のメールをお送りします。
    </p>
    <%= form_with(url: password_resets_path, scope: :password_reset,
                    local: true) do |f| %>
      <%= f.label :email, "メールアドレス" %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.submit "メールを送信", class: "btn btn-primary" %>
    <% end %>
    <!-- ==>  password_resets_controllerのcreateアクションへ-->
  </div>
</div>
